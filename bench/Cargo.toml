[package]
name = "bcs_bench"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bcs = "=0.1.6"
serde = { version = "=1.0.219", features = ["derive"] }
thiserror = "=1.0.68"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
